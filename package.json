{
  "name": "discord-downloader",
  "version": "0.1.0",
  "description": "Simple Discord bot to download all images and files with attribution (hopefully)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node .",
    "pm2start": "pm2 start index.js --name 'discord-downloader' --watch --ignore-watch='node_modules logs debug_message.json'; pm2 monit",
    "pm2stop": "pm2 stop 'discord-downloader'",
    "lint": "eslint ."
  },
  "author": "Chris Mills",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.0.2",
    "eslint": "^6.8.0",
    "moment": "^2.24.0",
    "winston": "^3.2.1"
  }
}
